###############################################################################
# General aliases
###############################################################################

# Tmux with 256 colors enabled
alias tmux='tmux -2'

# Dave keystrokes in todo.txt
alias t="todo.sh"

###############################################################################
# Apt aliases
###############################################################################

alias aptu='sudo apt-get update && sudo apt-get upgrade'

# Clean cache and remove orphan packages
alias aptc='sudo apt-get clean && sudo apt-get autoremove'

# Install package
alias apti='sudo apt-get install'

# Remove a package
alias aptr='sudo apt-get remove'

# Search for a package
alias apts='sudo apt-cache search'

###############################################################################
# Pacman aliases
###############################################################################

# Update repositories
alias pacu='sudo pacman -Syu'

# Clean cache
alias pacc='sudo pacman -Sc'

# Optimize pacman
alias pacopt='sudo pacman-optimize'

# Install package
alias paci='sudo pacman -S'

# Remove package and dependencies
alias pacr='sudo pacman -Rs'

# Search for a package
alias pacs='pacman -Ss'

# Display package information
alias pacinfo='pacman -Si'

# List all explicitly installed packages
alias pacls='pacman -Qe'

# List all manually installed packages
alias paclm='pacman -Qm'

# List all native installed packages
alias pacln='pacman -Qn'

# List files installed by a package
alias paclf='pacman -Ql'

# List installed packages by regex
alias pacli='pacman -Qs'

# List orphan packages
alias paclo='pacman -Qtd'

# Remove orphan packages
alias pacro='sudo pacman -Rns $(pacman -Qtdq)'
